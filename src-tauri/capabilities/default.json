{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions the widget and settings windows need: listening for app events (usage:poll-tick, settings:changed) and dragging the borderless widget window.",
  "windows": ["widget", "settings"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:window:allow-start-dragging"
  ]
}
